%h2 You're almost done! Now create a password to securely access your account.
= semantic_form_for(resource, :as => resource_name, :url => update_user_confirmation_path, method: :patch) do |form|
  = devise_error_messages!
  = form.inputs do
    = form.input :name, required: true, :input_html => {:autofocus => true}
    = form.input :location, label: "Location (ie. City, Region, Country)"
    = form.input :password
    = form.input :password_confirmation
    = hidden_field_tag :confirmation_token, @confirmation_token
  = form.actions do
    = form.action :submit, :label => 'Confirm Account'
