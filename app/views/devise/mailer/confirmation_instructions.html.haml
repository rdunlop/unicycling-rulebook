%p
  Welcome #{@resource.email}!
%p You can confirm your #{Rulebook.find_by(subdomain: Apartment::Tenant.current).try(:rulebook_name)} account email through the link below:
%p= link_to 'Confirm my account', confirmation_url(@resource, :confirmation_token => @token)
