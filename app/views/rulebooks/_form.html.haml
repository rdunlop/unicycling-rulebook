= form_for(@rulebook) do |f|
  - if @rulebook.errors.any?
    #error_explanation
      %h2
        = pluralize(@rulebook.errors.count, "error")
        prohibited this rulebook from being saved:
      %ul
        - @rulebook.errors.full_messages.each do |msg|
          %li= msg
  %table
    %thead
      %tr
        %th Name
        %th
        %th Explanation
    %tbody
      %tr
        %td= f.label :admin_upgrade_code
        %td= f.text_field :admin_upgrade_code
        %td Specify the code you will need to enter to upgrade your user account to an Admin account from the "Account Settings" page.
      %tr
        %td= f.label :rulebook_name
        %td= f.text_field :rulebook_name
        %td The name displayed on all pages/footers/etc
      %tr
        %td= f.label :subdomain
        %td= f.text_field :subdomain
        %td This is what will be displayed in the URL bar. (this cannot be modified after creation)
      %tr
        %td= f.label :front_page
        %td= f.text_area :front_page, class: "js--wysiwyg"
        %td The Text which will appear on the front page
      %tr
        %td= f.label :faq
        %td= f.text_area :faq, class: "js--wysiwyg"
        %td Text for the FAQ
      %tr
        %td= f.label :copyright
        %td= f.text_field :copyright, class: "js--wysiwyg"
        %td Copyright footer
      %tr
        %td.creation_code_field= label_tag :access_code, "Rulebook Creation Access Code"
        %td.creation_code_field= text_field_tag :access_code
        %td.creation_code_field This is necessary to submit this form successfully (get this from an IUF board member)
  .actions
    = f.submit
