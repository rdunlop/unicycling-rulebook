<% if show.new_record? 
    return 
end %>
<b><%= show.user %></b> voted <strong><%= show.vote %></strong>
 on <i><%= show.created_at.strftime("%B %d, %Y, %I:%M %p") %></i>
<% if not show.comment.blank? %>
    <blockquote><%= show.comment %></blockquote>
<% end %>
